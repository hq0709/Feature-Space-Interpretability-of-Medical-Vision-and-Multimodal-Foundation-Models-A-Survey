% the evidence map of \S\ref{sec:findings}: an upright two-column float
\begin{figure*}[!tbp]
\centering
\includegraphics[width=\linewidth]{fig6_findings.pdf}
\caption{The evidence map: for each statement of \S\ref{sec:findings}, the validity tests its records
report, one column per class in the codes of Table~\ref{tab:vcov}, as $k/n$ over the records whose design
can carry the class ($k_{\text{all}}$--$k/n$ where credited records differ in how much of their outcomes
the test covers); an en dash marks a class that does not apply. The randomized control task has no column
(none is reported; its applicability to image probes is not established, \S\ref{sec:future}) and
semantic validation none, because F7 selects on it. The studies behind each row are cited in Table~\ref{tab:statements}; no cell is combined with another, and none is graded.}
\label{fig:findings}
\end{figure*}
